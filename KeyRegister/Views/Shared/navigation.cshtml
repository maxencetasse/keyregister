@{
    // Titre
    string Title        = "Registre des clefs";

    // Lien
    string ProfilUrl    = Url.Action("Index", "Home");
    string IndexUrl     = Url.Action("Index", "Home");
    string CalendarUrl  = Url.Action("Index", "Home");
    string HistoryUrl   = Url.Action("Index", "Home");
}
<nav class="navbar navbar-default navbar-static-top" role="navigation" style="margin-bottom: 0">
    <!-- Titre en haut à gauche -->
    @Html.ActionLink(Title, "Index", "Home", null, new { @class = "navbar-brand" })

    <!-- Compte en haut à droite -->
    <ul class="nav navbar-top-links navbar-right" style="margin-right: 0px">
        <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#"><i class="fa fa-user fa-fw"></i><i class="fa fa-caret-down"></i></a>
            <ul class="dropdown-menu dropdown-user">
                <li>
                    <a href="@ProfilUrl">
                        <i class="fa fa-sign-out fa-fw text-danger"></i>
                        <span class="text-danger">Déconnexion</span>
                    </a>
                </li>
            </ul>
        </li>
    </ul>

    <!-- Barre à gauche -->
    <div class="navbar-default sidebar" role="navigation">
        <div class="sidebar-nav navbar-collapse">
            <ul class="nav nav-sidebar" id="side-menu">
                <li><a href="@IndexUrl"><i class="fa fa-dashboard fa-fw"></i> Tableau de bord</a></li>
                <li><a href="@CalendarUrl"><i class="fa fa-calendar fa-fw"></i> Plannings</a></li>
                <li><a href="@HistoryUrl"><i class="fa fa-history fa-fw"></i> Historique</a></li>
            </ul>
        </div>
    </div>
</nav>